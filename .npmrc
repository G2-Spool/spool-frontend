# npm configuration for CI/CD builds
legacy-peer-deps=true
# This helps with peer dependency resolution in CI environments
# and prevents build failures due to peer dependency warnings

# Additional settings for AWS Amplify builds
fund=false
audit=false

# Force npm to use the package-lock.json
package-lock=true
lockfile-version=3

# Ensure npm doesn't fail on peer dep warnings
force=true