{
  "name": "thread-graph-api",
  "version": "1.0.0",
  "description": "Neo4j Thread Graph API for Spool Learning Platform",
  "main": "src/index.js",
  "scripts": {
    "build": "zip -r threadGraph.zip src/ node_modules/",
    "test": "jest",
    "deploy": "./deploy.sh"
  },
  "dependencies": {
    "neo4j-driver": "^5.15.0",
    "aws-sdk": "^2.1520.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}